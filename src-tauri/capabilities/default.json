{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window (T4 adds the dialog plugin for the native binary-path picker)",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
